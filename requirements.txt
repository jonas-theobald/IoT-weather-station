adafruit-circuitpython-bme280>=2.6.0
flask>=2.0.0

# Hydris WiFi/gRPC publishing (see docs/HYDRIS_INTEGRATION.md).
# Optional at runtime -- if HYDRIS_SERVER isn't set, no transport is
# constructed and none of this is exercised. grpcio ships prebuilt wheels
# for armv7l/aarch64 (Pi Zero 2 W+) via piwheels; the original Pi Zero W
# (armv6l) may need a from-source build -- smoke-test on the target board.
grpcio>=1.60.0
protobuf>=5.0.0
platform-proto @ git+https://github.com/projectqai/proto.git#subdirectory=python

# BLE GATT peripheral (transport/ble_gatt.py): bluezero is NOT listed here
# on purpose. Its deps (PyGObject, dbus-python) fail to build in a venv, so
# install.sh apt-installs python3-gi/python3-dbus, exposes them via a .pth,
# and pip-installs bluezero --no-deps on top. Needs BlueZ >= 5.50 on the Pi
# (Bookworm ships 5.66). Only imported when HYDRIS_BLE=1.
